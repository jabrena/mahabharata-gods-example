## 0. Establish Deterministic Compatibility Evidence

- [x] 0.1 Add an HTTP-level characterization test that sends `GET /top-gods` with controlled downstream inputs and asserts status 200, `application/json`, exactly three `name` and `hitCount` objects, and the ordered Brahma (8100), Rama (845), and Hanuman (54) result.
- [x] 0.2 Replace the fixed WireMock port and random response delays with a dynamic port and deterministic fixtures while retaining the existing resource-delegation and controlled-data ranking checks.
- [x] 0.3 Replace the Spring Cloud Contract test stack with `org.wiremock:wiremock-standalone:3.13.2`, remove the unused Spring Cloud BOM and REST Assured dependency, and preserve the existing service-location configuration keys.
- [x] 0.4 Replace the three deprecated HTTP proxy constructions with `WebClientAdapter.create(...)` and `HttpServiceProxyFactory.builderFor(...)` without changing client interfaces, paths, codec limits, or asynchronous return types.
- [x] 0.5 Run `./mvnw --batch-mode --no-transfer-progress clean verify` and confirm the behavior-preserving preparation is green before changing the platform baseline.

## 1. Migrate the JVM Platform Through Verified Waypoints

- [x] 1.1 Upgrade to the latest Spring Boot 3.5.x maintenance release, resolve deprecation and dependency evidence without changing behavior, and run clean verification.
- [x] 1.2 Upgrade to the latest Spring Boot 4.0.x maintenance release, adopt the modular `spring-boot-starter-webflux-test` dependency, remove redundant direct JUnit, Mockito, AssertJ, and managed Commons Lang version overrides, and run clean verification.
- [x] 1.3 Configure the final Maven model for Java 25 and Spring Boot 4.1.0, resolve compatible managed dependency and plugin versions, and inspect the effective Maven model for baseline conflicts.
- [x] 1.4 Apply only source or configuration adaptations required by Spring Boot 4.1 while preserving `GET /top-gods`, the three service roles and paths, the existing service-location keys, and the `CompletableFuture` processing model.
- [x] 1.5 Update `.sdkmanrc` to a verified Java 25 distribution and update GitHub Actions to use Temurin 25 with `./mvnw --batch-mode --no-transfer-progress clean verify`.
- [x] 1.6 Confirm `pom.xml`, `.sdkmanrc`, and GitHub Actions consistently declare Java 25.
- [x] 1.7 Run `./mvnw --batch-mode --no-transfer-progress clean verify` on Java 25 and confirm the exact HTTP compatibility test, resource delegation, and ranking checks pass.

## 2. Verify the Native Executable

- [x] 2.1 Remove the custom GraalVM 22.3 requirement and redundant always-on reachability metadata configuration, retain the Spring Boot and Native Build Tools plugin declarations, and inherit the Spring Boot 4.1 native profile with Native Build Tools 1.1.1.
- [x] 2.2 Select and record a Java 25-compatible GraalVM distribution, run the documented native-image build command, and confirm it produces a native executable. GraalVM CE 25.0.2 (`~/.sdkman/candidates/java/25.0.2-graalce`, `native-image 25.0.2`) built `target/mahabharata-gods-native` via `./mvnw -Pnative -DskipTests clean native:compile` in 1m 34s (BUILD SUCCESS, total 1:45 min).
- [x] 2.3 Start the native executable with controlled downstream service locations, poll until ready, and confirm `GET /top-gods` returns status 200, `application/json`, and the exact ordered Brahma, Rama, and Hanuman payload. Started against a WireMock standalone 3.13.2 instance (fixed port 8090, fixtures matching the HTTP contract test) via the documented `SPRING_CLOUD_DISCOVERY_CLIENT_SIMPLE_INSTANCES_*_URI` overrides; the executable reported `Started Application in 0.049 seconds`, and `curl -D - http://localhost:8080/top-gods` returned `HTTP/1.1 200 OK`, `Content-Type: application/json`, and `[{"name":"Brahma","hitCount":8100},{"name":"Rama","hitCount":845},{"name":"Hanuman","hitCount":54}]`.
- [x] 2.4 Stop the native executable and controlled fixtures and record any remaining native compatibility limitation. Both processes were stopped and confirmed gone (`ps`/`lsof` checks on ports 8080/8090). No functional native compatibility limitation was observed; the JVM-based `native-image` build/run introduced no restricted-native-access warnings (contrast with the buildpack-built container image in 3.4, which does emit one at startup — see 3.4 note).

## 3. Verify Distinct JVM and Native Container Images

- [x] 3.1 Build a distinctly tagged JVM container image and record the resolved builder identity.
- [x] 3.2 Start the JVM image and controlled WireMock fixtures on an isolated container network, poll readiness, assert the exact `GET /top-gods` response, capture the exit result, and clean up all created containers and network resources.
- [x] 3.3 Build a distinctly tagged native container image and record the resolved builder identity without overwriting the JVM image. `./mvnw spring-boot:build-image -Pnative -Dspring-boot.build-image.imageName=mahabharata-gods-native:25-native` succeeded in 2:25 min, producing `mahabharata-gods-native:25-native` (328MB) alongside the untouched `mahabharata-gods-native:25-jvm`/`mahabharata-gods-jvm:25-jvm` (706MB). Resolved builder: Paketo `builder-noble-java-tiny`, stack `io.buildpacks.stacks.noble` (Ubuntu 24.04, run image `paketobuildpacks/ubuntu-noble-run-tiny:0.0.112`), buildpacks `paketo-buildpacks/native-image:5.18.6` and `paketo-buildpacks/bellsoft-liberica:11.8.2` (BellSoft Liberica NIK, Java 25.0.4 at container runtime).
- [x] 3.4 Start the native image and controlled WireMock fixtures on an isolated container network, poll readiness, assert the exact `GET /top-gods` response, capture the exit result, and clean up all created containers and network resources. Ran `mahabharata-gods-native:25-native` and `wiremock/wiremock:3.13.2` (mapping/`__files` fixtures matching the HTTP contract test) as `mahabharata-app-native` and `mahabharata-wiremock-native` on an isolated `mahabharata-native-net` bridge network. The app logged `Started Application in 0.041 seconds` (with a one-time JDK 25 `WARNING: A restricted method in java.lang.System has been called` from Netty's native library load — informational only, not a failure); `curl -D - http://localhost:18081/top-gods` (published from container port 8080) returned status 200, `application/json`, and the exact ordered Brahma (8100)/Rama (845)/Hanuman (54) payload. `docker stop` yielded exit code 143 (SIGTERM) for the app container. All containers and the network were removed (`docker rm`, `docker network rm`), confirmed by an empty `docker ps -a --filter name=mahabharata` and absent network listing.

## 4. Document and Reconcile the Verified Baseline

- [x] 4.1 Update `README.md` with the intentional Java 25 minimum, the verified SDKMAN/GraalVM prerequisites, and the verified JVM, native executable, JVM image, and native image build and run commands.
- [x] 4.2 Document distinct JVM/native image tags, controlled service-location arguments, readiness checks, and cleanup commands needed to replay container verification.
- [x] 4.3 Replay every README command against the final baseline and correct any command or version that does not match successful evidence. All README commands were replayed in this session: JVM `clean verify`, native build (`-Pnative -DskipTests clean native:compile`) and run, JVM container build/run, and native container build/run each matched the documented commands and produced the exact `GET /top-gods` contract; no README corrections were required.
- [x] 4.4 From the repository root run `./mvnw --batch-mode --no-transfer-progress clean verify`; from `documentation/` run `openspec validate --all --strict --no-interactive`; then record any remaining compatibility or reproducibility limitation before promoting the change. Both commands pass (re-verified after completing native/container evidence). Remaining limitation: none outstanding — native-executable and native-container verification (2.2-2.4, 3.3-3.4) are now evidenced; the JDK 25 restricted-native-access warning noted in 3.4 is informational only and does not block promotion.
